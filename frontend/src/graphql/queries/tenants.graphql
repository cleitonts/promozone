query GetTenants {
  tenants {
    edges {
      node {
        id
        name
        domain
        created
        updated
      }
    }
  }
}

query GetTenant($id: ID!) {
  tenant(id: $id) {
    id
    name
    domain
    created
    updated
  }
}