query GetProfiles {
  profiles {
    edges {
      node {
        id
        tenantId
        resolvers
        created
        updated
      }
    }
  }
}

query GetProfile($id: ID!) {
  profile(id: $id) {
    id
    tenantId
    resolvers
    created
    updated
  }
}