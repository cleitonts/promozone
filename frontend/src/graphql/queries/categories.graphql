query GetAllCategories {
  categories {
    edges {
      node {
        id
        name
        slug
        description
        active
        created
        updated
      }
    }
  }
}

query GetCategory($id: ID!) {
  category(id: $id) {
    id
    name
    slug
    description
    active
    created
    updated
  }
}